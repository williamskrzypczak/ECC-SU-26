# Chapter 3 Solution: Campus Event RSVP

## Setup

1. Add `EventCard.swift`, `RSVPButton.swift`, and `ContentView.swift` to your CampusEvents project.
2. Run the preview or simulator.

## What You Should See

- RSVP button toggles sign-up state and count
- Toggle, TextField, and segmented Picker all update the screen
- Green confirmation text after RSVP

## Files

- `EventCard.swift` — static card with passed-in values
- `RSVPButton.swift` — child view using `@Binding`
- `ContentView.swift` — parent view owning all `@State`

# Chapter 4 Solution: Campus Club Directory

## Setup

1. Add `Club.swift`, `ClubRow.swift`, and `ContentView.swift` to CampusEvents.
2. Set deployment target to iOS 16+ for `NavigationStack`, or replace with `NavigationView` on older targets.

## What You Should See

- Scrollable club list grouped by section letter
- Search field filters clubs by name
- Letter buttons jump to each section
- Done button dismisses the keyboard

## Files

- `Club.swift` — model and sample data
- `ClubRow.swift` — reusable row
- `ContentView.swift` — search, focus, and scroll-to-section
